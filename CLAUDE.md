# Fleet

Operating guide: `README.md`. Vocabulary: `CONTEXT.md`. Decisions: `docs/adr/`.

## Agent skills

### Issue tracker

Work on the fleet itself is tracked as local markdown under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each string equal to its name, recorded on a ticket's `Status:` line. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
